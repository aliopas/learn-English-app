# 🚀 خطوات تشغيل الموقع مع قاعدة البيانات الجديدة

## 1️⃣ تشغيل الـ Backend

```bash
cd backend
npm start
```

**المفروض تشوف:**
```
╔════════════════════════════════════════╗
║   🚀 Server running on port 5000      ║
║   📝 Environment: development         ║
║   🌐 Frontend URL: [URL]              ║
╚════════════════════════════════════════╝
```

---

## 2️⃣ تشغيل الـ Frontend (في terminal تاني)

```bash
cd project
npm run dev
```

**المفروض تشوف:**
```
  VITE v... ready in ... ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## 3️⃣ اختبار الموقع

1. **افتح المتصفح** على: `http://localhost:5173`
2. **سجل دخول** بحسابك
3. **روح على Roadmap** واختار اليوم الأول
4. **تأكد إن البيانات ظاهرة صح:**
   - ✅ الفيديو شغال
   - ✅ المفردات (10 كلمات) ظاهرة
   - ✅ القواعد (Google Doc) ظاهر
   - ✅ نص القراءة ظاهر
   - ✅ الملخص (الصورة) ظاهر
   - ✅ الاختبار (20 سؤال) شغال

---

## 🔍 معالجة الأخطاء

### إذا ظهر خطأ "Failed to load from database":
1. تأكد إن الـ backend شغال
2. تأكد إن الـ `.env` فيه بيانات الاتصال الصحيحة
3. شوف الـ console في الـ backend عشان تشوف الخطأ بالتفصيل

### إذا البيانات مش ظاهرة:
1. افتح Developer Tools (F12)
2. شوف الـ Network tab
3. تأكد إن الـ API call لـ `/api/lessons/1` راجع بيانات صح

### إذا الكويز مش شغال:
1. تأكد إن الأسئلة فيها `answer` أو `correct_answer`
2. شوف الـ console في المتصفح لأي أخطاء

---

## 📊 التحقق من البيانات في قاعدة البيانات

يمكنك استخدام Drizzle Studio للتحقق:

```bash
cd project
npx drizzle-kit studio
```

ثم افتح: `https://local.drizzle.studio`

---

## ✅ Checklist

- [ ] الـ Backend شغال على port 5000
- [ ] الـ Frontend شغال على port 5173
- [ ] البيانات ظاهرة في صفحة الدرس
- [ ] الكويز بيشتغل صح
- [ ] التقدم بيتحفظ في قاعدة البيانات

---

## 🎯 الخطوات التالية

بعد ما تتأكد إن اليوم الأول شغال:
1. أضف بيانات باقي الأيام (Day 2, 3, 4...)
2. اختبر كل يوم
3. تأكد إن التقدم بيتحفظ صح

---

**🎉 بالتوفيق!**
