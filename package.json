{
    "name": "english-learning-platform-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "cd backend && npm install && cd ../project && npm install",
        "build": "cd backend && npm install && cd ../project && npm install && npm run build",
        "start": "cd backend && node server.js"
    },
    "engines": {
        "node": "18.x"
    }
}